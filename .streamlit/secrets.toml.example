# Copy this to .streamlit/secrets.toml for LOCAL testing of the Streamlit-
# secrets code path only. On Streamlit Community Cloud, you paste this same
# content into the app's "Settings -> Secrets" box in the dashboard instead
# of committing a file -- see DEPLOYMENT.md.
#
# .streamlit/secrets.toml is already gitignored; this template file (with
# a placeholder value, not a real key) is safe to commit.

API_BASE_URL = "https://your-backend-service.onrender.com"
